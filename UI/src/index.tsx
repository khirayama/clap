import * as React from 'react';
import * as ReactDOM from 'react-dom';

window.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<div>OK</div>, window.document.querySelector('#root'));
});
